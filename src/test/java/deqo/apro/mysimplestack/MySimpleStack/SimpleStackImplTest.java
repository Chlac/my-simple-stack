package deqo.apro.mysimplestack.MySimpleStack;

import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;

import java.util.Stack;

import static org.junit.Assert.*;

public class SimpleStackImplTest {

    /*SimpleStack stack;

    @Before
    public void setUp() throws Exception {
    }

    @Test
    public void testCreateEmptyStack () { // Test case
    // Code under test
        SimpleStackImpl stack = new SimpleStackImpl();// Assertions ( oracle );
        assertNotEquals(stack, null);
        assertTrue ("A new stack should be empty ", stack.isEmpty());
        assertEquals ("A new stack has no element" ,0 , stack.getSize());
    }

    @Test
    public void isEmpty() throws Exception {
        Assert.assertEquals(true, stack.isEmpty());
        String e = "toto";
        stack.push(new Item(new String(e)));
        Assert.assertEquals(false, stack.isEmpty());
    }

    @Test
    public void getSize() {
    }

    @Test
    public void push() throws EmptyStackException {

        // Setup the " state of the world "
        Stack stack = new SimpleStack();
        Item item = new SimpleItem();
        // Code under test
        stack . push ( item );
        // assertions ( oracle )
        assertFalse (" The stack must be not empty ", stack . isEmpty ());
        assertEquals ("The stack shoult contain 1 item",
                1, stack.getSize ());
        assertSame (" Pushed itm should be on top of stack " ,
                item , stack . peek ());

    }

    @Test
    public void peek() {
    }

    @Test(expected = EmptyStackException.class)
    public void pop() {
        // Setup the " state of the world "
        SimpleStackImpl stack = new SimpleStackImpl();
        // Code under test
        stack.pop(); // should throws an EmptyStackException .
    }}
JUnit-Introduction M1 Inf
    }
    */
}